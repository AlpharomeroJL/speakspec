# Release assets

- `hero.png` — product hero image for README and GitHub social preview.
- `demo.gif` — record before v1.0.0 release:

  1. Launch Speakspec from the release installer.
  2. Record a ~90s system description.
  3. Walk constraint review → interview → generate → results.
  4. Copy AGENTS.md and paste into Claude Code; show scaffolding.

  Recommended tool: ScreenToGif (Windows). Target size: under 5 MB, 720px wide.
